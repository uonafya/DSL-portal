# Maps query parameters placeholders(eg @ward@) keys to varibale name gotten from ui
# The property key here is matched against a filter name from the http request to get query parameter to replace with http request filter values
ward=@ward@
constituency=@constituency@
county=(@county@)
cadre=(@cadre@)
cadre_group=(@cadre_group@)